def insertion_sort(myList):
	n = len(myList)
	for j in range(1, n):
		key = myList[j]
		i = j
		while i > 0 and myList[i-1] > key:
			myList[i] = myList[i-1]
			i = i-1
		myList[i] = key;
	return myList
